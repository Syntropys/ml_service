# === Core Framework ===
fastapi>=0.109.0
uvicorn>=0.27.0
pydantic>=2.5.0
pydantic-settings>=2.1.0
python-multipart>=0.0.9

# === Auth ===
PyJWT>=2.8.0

# === Monitoring ===
prometheus-client>=0.20.0

# === Database ===
supabase>=2.0.0

# === ML — Disease Detection (TFLite) ===
# Use tflite-runtime (lightweight, ~5 MB) instead of full tensorflow (~800 MB)
# On Railway free tier, full TF would exceed 512 MB RAM limit
tflite-runtime>=2.14.0; platform_machine != 'aarch64'
numpy>=1.26.0
Pillow>=10.0.0

# === ML — Predictive Analytics (XGBoost) ===
xgboost>=2.0.0
scikit-learn>=1.4.0
joblib>=1.3.0
pandas>=2.2.0
